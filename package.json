{
  "name": "meta-lead-integration",
  "version": "1.0.0",
  "private": true,
  "description": "Meta Lead Ads -> CRM integration. Serverless on Vercel; Postgres (Neon) is the source of truth.",
  "scripts": {
    "start:dev": "vercel dev",
    "build": "tsc --noEmit && tsx scripts/migrate.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx scripts/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "setup:schedules": "tsx scripts/setup-schedules.ts",
    "create:platform-admin": "tsx scripts/create-platform-admin.ts",
    "bootstrap": "bash scripts/bootstrap.sh",
    "gen:secrets": "node scripts/generate-secrets.cjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.1",
    "@upstash/qstash": "^2.7.17",
    "@upstash/redis": "^1.34.3",
    "@vercel/node": "^5.1.8",
    "bcryptjs": "^2.4.3",
    "drizzle-orm": "^0.45.2",
    "jose": "^5.9.6",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.14.9",
    "@types/pg": "^8.11.10",
    "@vercel/static-build": "^2.0.6",
    "dotenv": "^16.4.5",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vercel": "^54.17.3",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
